# 更新日志

## 0.1.0-diagnostic — 獨立 Mod

- 建立獨立 `PalSkillDPSAnalyzerSP` 套件與全新 Workshop metadata，不沿用上游 Published File ID。
- 定位改為傷害驗證工具，不輸出玩家比較排名。
- 預設只納入帕魯；人物／武器來源可由 `IncludePlayerDamage` 選擇性開啟。
- 依技能欄位、投射物或傷害來源建立候選桶，輸出總傷害、占比、整場 DPS、命中與平均每擊。
- 啟動時反射傷害事件 schema，逐候選保留有界樣本；未知資料不猜名稱。
- 新增帕魯技能與人物武器候選回歸測試。

## 3.4.0

- 新增语义明确的 `EnablePalDamageBreakdown` 开关，用于显示玩家角色和每只参战帕鲁的伤害、占比及 DPS。
- 帕鲁明细继续优先显示玩家自定义昵称，未设置时回退物种本地化名称。
- 保留 `EnableTeamDetails` 作为旧配置兼容别名，两者任一开启即可显示同一份明细。
- 专用服务器默认关闭逐只帕鲁明细，避免公共聊天刷屏。
- 文档明确区分 Boss 战斗场地与 PvP 竞技场的支持边界。

## 1.2.0 — Steam 创意工坊单机版

- 单机版默认开启逐只帕鲁伤害明细，攻击 Boss 后无需再猜测旧配置项名称。
- 配置文件提供 `EnablePalDamageBreakdown` 独立开关，设为 `false` 可恢复精简结算。
- 创意工坊多语言说明补充开启方式和竞技场支持范围。

## 3.3.0

- 新增 Palworld 官方全部 17 种语言的游戏内核心战报。
- 单机/房主版通过 Unreal 当前语言自动选择翻译，无法识别时回退英语。
- 新增命名占位符模板，允许各语言独立调整语序，不再依赖中文字符串拼接。
- Boss 和帕鲁名称继续优先读取游戏本地化数据库；配置覆盖支持按语言分别设置。
- Steam 创意工坊单机版升级到 v1.1.0，新增 17 种本地化标题、描述和自动校验清单。
- 英语改为 Steam 页面正式回退语言，避免中文内容被错误写入英文栏。

## 3.2.0

- 新增可选 `BossDPSNativeCollector` C++ 采集器，高频回调只读取并聚合伤害字段。
- Lua 改为定时拉取聚合桶，继续负责 Boss 判断、玩家/帕鲁归属、捕捉/死亡结算和聊天战报。
- 普通目标经 Lua 首次判定后回传 C++ 负缓存，后续伤害不再进入 Lua。
- 原生反射或 ABI 不兼容时默认自动回退到原有 Lua 伤害钩子，并在启动日志中明确显示采集模式。
- 聚合记录保留精确总伤害和命中次数；新增 200 万次并发原生压力测试及 Lua 原生桥接回归。

## 1.1.0 — Steam 创意工坊单机版

- 新增符合 Palworld 1.0 `Info.json`/Lua 安装规则的创意工坊内容包，并显式依赖 `UE4SSExperimentalPW`。
- 新增 `LocalOnlyMessages`：单机/房主安装只向本机参与者显示战报，多人情况下无法确定本机身份时停止发送。
- 新增创意工坊封面、中文介绍、构建脚本和包结构验证。
- 新增 17 种游戏内语言以及对应的创意工坊标题和描述。

## 3.1.0

- 将月亮领主的身体、头部和左右部件合并为一场遭遇，只在主体结束时统一结算。
- 使用共同 Owner/父 Actor 隔离同时存在的复合 Boss；缺失关系时使用短加入窗口兜底。
- 调整伤害热路径，先判断受击目标是否为 Boss，再解析玩家与帕鲁归属。
- 增加普通目标负缓存，避免全服普通战斗反复执行完整归属查询。
- 缓存攻击来源、玩家身份和每场逐只帕鲁元数据，降低多段技能的游戏线程反射开销。
- 增加复合 Boss、并行复合遭遇、千次多段命中以及普通目标快速路径回归测试。

## 3.0.0

- 默认改为低打扰结算：摘要、MVP 和玩家综合排名。
- 默认保留一行开始统计提示，方便确认模组已识别 Boss 战。
- 新增 DPS 总开关以及开始提示、实时战况、详细奖项、队内明细和趣味点评的独立开关。
- 修复多人参与时同一消息按人数重复显示的问题。
- 修复旁观者收到其他队伍 Boss 战报的问题。
- 使用统一的 `PalUtility:PalCaptureSuccess` 捕捉入口并保留特殊战斗兼容监听。
- 无伤害超时缩短为 60 至 70 秒。
- 改进坐骑技能、玩家角色和逐只帕鲁伤害归属。
- 增加默认精简模式、开关隔离、收件数组和压力测试。
