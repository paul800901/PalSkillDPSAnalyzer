# 更新日志

## 0.5.19-core-hud — 原生設定、清楚的施放統計與 Boss 結算快照

- F3 改由原生 CommonUI／LogicMods 開啟可點擊的設定與「本次測試詳情」頁；外部 HUD 維持不可點擊的五項核心顯示層，F1 保留不用，F2 負責重設。
- 詳細頁統一使用「累計傷害、總命中段數、有傷施放、無傷施放、每次施放命中段數」等用語；最低／平均／最高明確標示包含無傷施放，歷次最高與理論上限也改成可直接理解的名稱。
- Boss-only 手動測試在最終 Boss 死亡或捕捉的瞬間凍結結果；已知階段切換不提前結算。所有野生帕魯範圍不因目標死亡凍結，持續到下一次 F2。
- Boss 結算後鎖定完整快照直到 F2；死亡事件後才抵達的多段技能尾傷會被忽略，不會另開一場並用單一技能覆蓋完整結果。
- 手動 Boss 的主 HUD 快照不再套用 15 秒自動分場逾時；結算後所有欄位維持原值，且 F3 內切換測量方式或目標範圍只套用到下一次測試，不會暗中清除目前結果。只有 F2 會歸零。
- 詳細資料由同一份顯示模型供原生與外部頁面使用，長清單標示「最近 N 次／共 N 次」，避免兩套 UI 詞彙與數值格式分歧。
- 公開版預設停用大型原生探針傾印與固定命中數檢查點，保留必要摘要，降低 UE4SS.log 噪音。

## 0.5.18-reverse-pair-probe — 傷害先到、技能來源後到的唯一反向配對

- 實機確認部分多段／脫手技能的 `ActualDamage` 會早於可辨識的 Blueprint OnAttack 技能來源，因此同步呼叫鏈無法涵蓋所有 Boss 命中。
- 新增有界反向配對：尚未歸屬的實際傷害最多暫存 1 秒；後續來源只有在攻擊者與 Boss 目標完全相同、且等待中的傷害恰好一筆時，才補回該技能。
- 同一攻擊者與 Boss 同時有兩筆以上待配傷害、物件世代不符、來源衝突、逾時或佇列滿載時一律維持「未辨識傷害」；不使用傷害大小、倍率、元素、目前動作、最近施放或三格技能猜測。
- 未配對傷害逾時後仍原值送入統計，唯一配對只改變技能桶、不改變 `ActualDamage` 與命中數；新增唯一、歧義、物件世代、逾時、容量與傷害守恆測試。
- 本版是實驗性結構配對，仍需 Boss 實測以 `reverse_pair_promoted`／`ambiguous`／`expired` 與最終技能分布判定是否可正式採用。

## 0.5.17-native-damage-utility-probe — 技能主命中傷害處理鏈診斷

- 實機證實上一版 `PalAttackFilter:CallBackOnAttackDelegate` 雖可註冊，但受控 Boss 場的呼叫次數為 0；因此不再把它視為技能本體主命中的來源入口。
- 新增原生 `PalUtility:ProcessDamageAndPlayEffectsByDamageInfo` Pre／Post 探針，沿當下引擎呼叫堆疊尋找實際 `PalAttackFilter`／技能效果，直接讀取 Filter 的 Waza、攻擊者與目標。
- 只有傷害處理呼叫與 final damage 同步巢狀、Filter Waza 完整、攻擊者與 Boss 目標全部一致時，才輸出精確技能；多個來源衝突、欄位缺失或呼叫鏈未接上時仍保留「未辨識傷害」。
- 不使用目前動作、最近施放、時間窗、傷害倍率、元素或面板技能組來填補歸因；新增的診斷資料有固定樣本、堆疊深度與輸出大小上限。
- 原生建置、200 萬命中壓力測試、事件佇列與來源鏈回歸已通過；這個新入口是否涵蓋遊戲內技能主命中，仍需本版實機測試確認。

## 0.5.16-native-filter-callback-probe — 技能主命中來源診斷

- 新增原生 `PalAttackFilter:CallBackOnAttackDelegate` Pre／Post 探針，直接從 Filter 讀取 Waza、攻擊者與 OwnerActionId，驗證技能本體主命中是否在同一引擎呼叫鏈內進入最終 OnDamage。
- 只有 Filter 回呼與 final damage 同步巢狀、攻擊者／Boss 目標一致且 Waza 完整時，才允許成為精確來源；未通過者維持「未辨識傷害」。
- 對前 24 筆未歸屬 final hit 加入有限診斷：最多 8 層 Blueprint 呼叫堆疊、實際 UObject／FWeakObject 來源欄位與直接 Waza／Skill／Action／Effect ID；不讀取目前動作、最近施放、時間、倍率或元素來猜技能。
- 上一版受控 Boss 場確認逐擊事件無掉落、無溢位且 128 次命中守恆；98 次已有精確／一致來源，剩餘 30 次皆為沒有 OnAttack scope 的第二條主命中路徑。本版用於確認該路徑後再決定正式接入方式。

## 0.5.15-native-pair-link — 引擎命中配對正式歸因

- 將實機驗證的 `PalSkillEffect OnAttack` 攻擊者＋Boss 目標配對正式接入技能桶：只有唯一來源，或同時間多筆來源全部同意同一 action／effect／Waza 時才歸因。
- 來源互相衝突、缺失、DamageInfo 指紋候選與技能倍率／元素仍一律留在「未辨識傷害」，不使用目前動作、最近施放或時間窗猜技能。
- 擴充原生 Blueprint 傷害處理器辨識到 hit／overlap／collision／impact／burst／explode 名稱，針對 BlastCannon 類投射物與爆炸路徑收集同樣的 Defender＋FPalDamageInfo＋Attacker 強結構證據。
- 兩組實機 Boss 測試共 251 次命中中，唯一或一致來源為 212 次、歧義 0 次；其餘 39 次仍保留未歸屬。本版將這 212 類型由診斷候選提升為正式歸因。
- 原生收集器版本更新為 `3.7.0-pair-link`；新增唯一配對、一致配對、歧義 fail-closed 與傷害守恆回歸。原生每次排空上限維持 512。

## 0.5.14-native-exact-attribution — 原生逐擊精確來源鏈

- 原生 C++ 收集器改為逐擊 Event v2，沿 action／cast、技能效果與子效果、AttackFilter Waza、Blueprint OnAttack 連到最終 OnDamage；歸因完成前不先聚合。
- Blueprint script UFunction 改由 UE4SS 全域 ProcessLocalScriptFunction callback 攔截；OnInitialize／BindPrimitiveComponent 未被觀察到時不再錯誤停用整條事件流。
- 原生事件只有精確 effect／cast／Waza 證據可進入具名技能桶；缺少來源、來源衝突、攻擊者不符或物件世代不符時一律 fail closed 到「未辨識傷害」。
- 移除原生 Event v2 對目前動作、最近施放、三格唯一倍率、BasePower 與元素推測的依賴，避免多段脫手技能互相冒名。
- 新增冰河凍域／沙塵暴／啟示錄／暗能彈交錯延遲命中、同倍率同屬性、DamageInfo copy、物件位址重用、狀態傷害隔離與逐擊守恆回歸。
- 原生建置已通過 200 萬命中壓力測試、事件佇列與來源鏈測試；完整離線測試及本機安裝檔 hash 讀回通過。Palworld 實機精確命中尚待本版重開驗收；灼燒／中毒獨立狀態列仍未完成。

## 0.5.13-hybrid-attribution — 即時三格技能與施放生命週期

- 每次命中前即時維護帕魯的三格 `EquipWaza`，換技後由下一次 action 重新讀取，不沿用舊配置。
- 精確 DamageInfo／效果／Waza 證據仍優先；缺失時才以三格裝備、action begin/end 與三格內唯一倍率＋元素作有限推定。
- 三格外 AI 補招歸為普攻；推定結果不反向訓練精確簽名，同時多招或過期命中無法唯一判斷時維持未歸屬。
- 新增推定／精確傷害分流與換技、延遲命中、火／冰／暗系及同時技能衝突回歸。
- 狀態：離線全套驗證通過；仍需 Palworld 實機確認換技、延遲技能與普攻分流。灼燒／中毒獨立狀態列尚未完成。

## 0.5.12-exact-source-chain — 精確施放／效果來源鏈

- 移除 BasePower＋元素、目前動作、最近施放與短時間窗對正式技能桶的決定權；缺乏強來源時一律保留「未辨識傷害」。
- 每次 action begin 建立獨立 cast，並沿 `PalSkillEffectBase:OnInitialize`、`PalAttackFilter:BindPrimitiveComponent`、效果 `AttackFilter.Waza`、Blueprint OnAttack 與最終 OnDamage 傳遞來源。
- Blueprint OnAttack 改用 UE4SS `RegisterCustomEvent`；`RegisterHook` 官方不支援 delegate，因此不再把成功註冊誤當成成功收到命中。
- 支援 action 事件尚在佇列、effect 已同步建立的順序：以捕捉時間與 actor＋Waza 回補該 cast，不靠命中倍率猜技能。
- 新增交錯延遲技能、同倍率同屬性、DamageInfo wrapper copy、OnAttack／OnDamage 巢狀命中、一次性 token 與重設清理回歸。
- 狀態：離線全套驗證通過；Palworld 實機需確認 effect-init、effect-attack、effect-hit-match 三段日誌後才算 live 完成。

## 0.5.11-dark-attribution-hotfix — 暗系長戰固定簽名

- 從世界樹之龍 10 分 48 秒長戰日誌確認夜幕魔蝠三格為暗黑雷射／黑暗之擁／劇毒射擊，加入 450／闇、600／闇、30／闇的三格裝備限定簽名。
- 修正三個技能在同時動作或延遲命中時被拆進「未辨識傷害」；40／闇仍保留為普攻「暗能彈」。
- 新增同時裝備技能衝突與暗能彈隔離回歸，要求已知四列加總等於整場總傷且不殘留未辨識。
- 中毒狀態仍是獨立收集缺口，不把無施加者證據的傷害偽裝成劇毒射擊。
- 本輪重附影片建立於暗系戰鬥前，未宣稱完成暗系逐幀驗證；修正依完整 UE4SS 最終傷害日誌與畫面終值完成。

## 0.5.10-fire-attribution-hotfix — 火系多段傷害與普攻隔離

- 依 Palworld 1.0 技能資料與 2026-08-13 兩場實機最終傷害事件，加入烈焰球 600／火、流火 300／火、烈焰風暴 200／火的三格裝備限定簽名。
- 沒有 `WazaID`／`DamageCauser` 的火系多段命中，可在當下三格內唯一符合時直接歸回原技能，不再先形成大量未歸屬列。
- 目前動作為暗能彈且事件本身是 40／闇時，保留為「普攻｜暗能彈」；不再被上一個已完成火系技能的延遲視窗搶走。
- 新增火系三技能、延遲火系效果與暗能彈同場的回歸測試，驗證技能列加總等於整場總傷。
- 灼燒仍是獨立收集缺口：兩場現行 final-damage 日誌沒有帶入可歸屬的灼燒事件，暫不把不明傷害偽裝為灼燒。

## 0.5.10-position-attribution-regression — 雙重定位與晶鑽之雨完整歸因

- 從 23:03 實機 overlay log 確認同一 sequence 有兩個定位者互拉：傷害快照排入的 Dispatcher closure 收到空 anchor，先把視窗移到右側；200 ms timer 再依 `left-center` 拉回左側。現在傷害快照完全不再定位，只有 timer 擁有位置，且相同 anchor／工作區／寬度不會重寫座標。
- 修正每次 PalAction begin 都刪除未歸屬候選的資料遺失。動作開始現在只要求重讀 EquipWaza；只有實際三格技能指紋改變才清除歸因映射，累積傷害列永不因一般施放被刪除。
- 依 23:03 實機三格 `DiamondFall / DoubleIcicleThrow / IcicleThrow` 與最終傷害 `BasePower / AttackElementType` 建立限定於該帕魯當下裝備技能的唯一簽名：BP600 冰傷歸晶鑽之雨、BP700 冰傷歸極寒雙星、BP450 冰傷歸鑽石星辰。這會把與其他技能同時落地、先前標成 conflict 的延遲命中合併回原技能。
- 新增位置唯一寫入回歸（兩次快照只允許一次實際 position move）與總傷守恆回歸（動作開始後，來源總傷仍必須等於所有技能候選傷害總和）。
- 狀態：離線全套測試通過並可建包；Palworld 實機尚待下一場驗收。F1 原生設定頁仍未完成，不列為本版已修功能。

## 0.5.9-attribution-failsafe-diagnostic — 技能來源串接與輸入止血

- **HUD 改為建立一次、原地更新**：原本每次 0.5 秒快照都會清空整個 UI 樹、重建全部文字／長條／技能列並重算視窗位置，造成有傷害時左右跳動。現在視窗與技能列只建立一次，資料更新只修改既有文字與長條寬度，只有換帕魯／換技能時才增刪列；左上座標只在啟動、解析度或錨點變更時設定。離線驗證 `meter_rebuilds=1`（連續兩次快照只建立一次）。
- **技能簽名表只收可靠證據**：只有直接 Waza、DamageInfo 身份、效果資產、Waza marker 與目前動作可建立簽名；「最近完成施放」的時間推定只能累計顯示傷害，不得污染簽名表（避免一個 BasePower＋屬性對應多個技能）。簽名以每隻帕魯＋目前三格裝備技能為單位，換技後清除舊簽名與未歸屬桶。
- 修正換技後分類停滯：三格裝備技能清單原本只讀一次，換成極寒雙星／鑽石星辰後仍用舊清單把新技能誤標成「普攻」。現在每次動作開始會失效快取，且每 `EquipWazaRefreshSeconds`（預設 5 秒）強制重讀；新裝備的技能在下次命中即恢復為正式技能名稱。
- F2 重設加入 0.5 秒防連按（長按鍵不會重複開新測試），且重設後顯示可見的「等待命中」畫面，不再直接隱藏讓玩家看不出是否生效。
- F1 因原生設定頁未完成而拒絕開啟時，會在 HUD 顯示 3 秒「設定功能尚未提供」提示（17 語言），不再靜默無反應。
- HUD 技能列小字改為明示「命中／施放」計數（17 語言標籤），不再用無標題的 `×施放數` 誤導（未歸屬傷害不再看起來是 0 命中）。
- DamageInfo 身份在最終傷害中無法匹配到 Waza 建立事件時，新增 `damage-info-unmatched` trace，實機可驗證 ReturnValue 是否被複製／包裝，而非靜默降級成時間推定。
- 依 UE4SS 實際 post-hook 呼叫順序修正 `MakeDamageInfoByWazaType`：ReturnValue／DamageInfo 位於 Context 後、原始參數前；舊版參數全部錯位，導致 Waza marker 從未建立。
- 將 Waza 建立事件的 ReturnValue／DamageInfo 身份帶到最終傷害事件，並使用有界 marker 佇列處理同時技能與延遲落地；目前動作降為弱備援，不得再把晶鑽之雨冒名成暗能彈。
- 外部 HUD 改為固定尺寸、不可點擊的純顯示層；停用跨行程搶焦點、游標解除裁切、Input Disable 與週期性重新定位。
- F1 外部互動設定暫時 fail closed，等待遊戲內原生 CommonUI／UMG；新增 F2 安全歸零，不開外部視窗也不改遊戲游標或輸入模式。
- v0.5.8 已由實機影片與日誌判定不通過，不可視為已修復版本。

## 0.5.8-click-input-attribution-hotfix — 面板點擊與延遲命中熱修

- 修正 F1 視窗移除 `WS_EX_TRANSPARENT`／`WS_EX_NOACTIVATE` 後，Windows 尚未立即重算視窗樣式，導致游標可見但點擊穿透回遊戲；現在會即時套用框架變更、取得鍵盤焦點，並在設定開啟期間維持游標解除裁切。
- 只有帶有效 Waza 或明確技能 Blueprint 的目前動作能成為技能列；側步、移動與純動畫不再被當成攻擊技能。
- 晶鑽之雨等延遲命中若落下時帕魯正在側步，會忽略側步狀態並回查最近完成的真實技能；內建補招仍獨立標為「普攻｜技能名」。
- 以 2026-08-12 實機日誌加入回歸測試：`PalAction_AnimationStepRight` 期間的冰雨命中必須合併回 `DiamondFall`，不得出現側步技能列。

## 0.5.7-delayed-effects-cursor-menu — 延遲效果、可見游標與選單隱藏

- 從 Palworld 1.0 熟化資產確認 `DiamondFall` 的落下命中與落地爆炸皆直接宣告同一 Waza、各為 0.05 倍威力；新增 DamageCauser 資產規則，兩段固定併入「晶鑽之雨」，且不會被同時進行的其他技能目前動作覆蓋。

- 新增通用延遲效果歸屬：保留每隻帕魯最近完成的每次施放，冰雨、落地爆炸、延遲投射物與持續地板命中可依直接 Waza、動作實例、最近非衝突施放與短期命中特徵回到原技能。
- 同時間有兩個施放都可能造成該命中時不強行猜測，統一標示為「未歸屬傷害」；不再用「狀態／其他」暗示它一定是燃燒或異常狀態。
- F1 開啟時同步要求 Palworld 與 WPF 顯示滑鼠、解除既有游標裁切；關閉時恢復原本遊戲游標與裁切範圍。
- 原生暫停／設定選單、標題畫面、讀取中或沒有可控制角色時自動隱藏 DPS，回到遊戲後恢復最近一次儀表。
- 修正 WPF Dispatcher 將空值傳入布林參數造成的例外，避免互動狀態中斷。
- 裝備技能診斷 log 額外列出實際三個 Waza 代碼，方便下一次實機驗證延遲技能。

## 0.5.6-interaction-stability — 分頁穩定與完整輸入鎖

- 戰鬥儀表與「本次測試」改以總傷害作為主要大數字，DPS 降為次要效率資訊；停止攻擊後仍能直觀看出每個技能實際累積了多少傷害。
- 修正「本次測試」在外部狀態更新後跳回「設定」：卸載舊分頁時產生的 `SelectedIndex=-1` 不再覆蓋使用者選擇。
- F1 工作區開啟期間停止接受週期性 DPS／時間快照重建；最新統計仍持續保留，於下一次明確操作或重新開啟時顯示，避免面板閃爍。
- WPF 只在面板首次開啟時取得焦點，後續置頂檢查不再每秒重新啟用視窗。
- 除鏡頭與移動外，同時停用 PlayerController 與受控 Pawn 的動作輸入，關閉面板時配對恢復，避免點擊設定時投擲手榴彈或觸發攻擊。
- 以 2026-08-12 實機紀錄加入「極寒雙星」回歸資料：4,280 傷害、29 次命中、4 次施放；分頁重建後仍須顯示該列。

## 0.5.5-fixed-workspace — 固定設定工作區尺寸

- F1「設定」與「本次測試」改為共用同一個固定工作區；切換分頁時只替換內容，不再因結果筆數或空白狀態改變整個視窗大小。
- 一般螢幕固定為 640×760；較矮的工作區會保留 80 像素安全距離並自動縮短，頁內內容以捲動方式呈現。
- 新增有資料與空白結果兩種 WPF 版面驗證，要求兩者的實際期望尺寸完全相同。

## 0.5.4-launch-hotfix — 停止黑框重啟循環

- 修正 UE4SS 內 `os.execute` 對長引號命令解析失敗而每五秒重試的問題：Lua 現在只呼叫無參數的短啟動命令，由啟動器自行解析同目錄檔案。
- 加入硬性失敗上限；若 HUD 仍無心跳，同次遊戲最多重試三次後停止，不再允許無限黑框閃現與週期性卡頓。
- 以實際短命令驗證隱藏 WPF 程序可建立心跳，並在 Palworld 不存在時自行結束及清理心跳。

## 0.5.3-attribution-ui — 三技能分類、多段合併與設定穩定

- 讀取每隻帕魯實際裝備的 `EquipWaza` 三格：裝備中的招式維持技能名稱，冷卻時由遊戲補出的非裝備攻擊改標示為「普攻｜技能名稱」。
- 即時 HUD 也會把同一 `BasePower + AttackElementType` 的延遲彈、地面爆炸與後續小傷合併回唯一相符的原技能，不再等測試結束才合併；若同時符合多招則保留未解析，避免誤算。
- 無法唯一歸因的灼燒、異常或模糊傷害統一顯示為「狀態／其他」，預設隱藏內部英文識別碼，避免被誤認為第四個技能。
- F1 設定更新時保留目前分頁與兩頁各自的捲動位置，先建立完整的新畫面再一次替換，修正「本次測試」跳回設定及閃動。
- 外部 WPF HUD 改由隱藏且脫離主控台的 VBScript 啟動器開啟，不再產生持續存在的黑色 Windows Terminal 視窗。
- 新增普攻分類、多段技能即時合併、17 語言分類文字、啟動器與分頁狀態回歸驗證。

## 0.5.2-hud-recovery — 顯示復原、設定鎖與快速重設

- 外部 HUD 新增心跳、錯誤記錄與 Lua 端 watchdog；顯示程序中止時會自動重新啟動，不再必須重開 Palworld。
- WPF 計時器與 Dispatcher 例外改為記錄並復原，返回遊戲前景時會重新確認置頂狀態。
- F1 設定開啟期間由遊戲執行緒鎖定人物移動與鏡頭輸入；F1、Esc、關閉或顯示復原失敗都會配對解除，避免影響主畫面操作。
- 「開始新測試」移到設定頁頂部固定操作列，按下後立即停用按鈕並顯示重新待命提示。
- 滑鼠命令改用具識別碼、回執與去重的單筆佇列，避免狀態檔同時讀寫造成重設遺失或重複執行。
- 保留手動測試的原始語意：重設前可跨一般野生帕魯、Boss 與多個目標持續累計，不會因進入 Boss 戰自動清空。

## 0.5.1-damage-lab — 手動測試、多目標與互動設定

- 新增「手動測試區間」：可在 F1 隨時開始新測試並歸零；計時從第一筆有效傷害開始，不會因其中一個目標死亡而提早結束。
- 目標範圍預設擴充為所有野生帕魯，涵蓋一般大世界戰鬥、野外／石板 Boss 與基地多帕魯群戰；仍可切回只統計 Boss。
- 同一測試區間依實際帕魯個體分組，再依技能分桶，讓坐騎、隊伍／跟隨帕魯與多隻基地帕魯的結果彼此獨立。
- F1 改為可隨時操作的 WPF 工作區，支援滑鼠與鍵盤，並加入「設定」與「本次測試」分頁；結果分頁可捲動查看所有來源與技能。
- 即時儀表移至左側安全區、預設縮放 85%，只保留前五個技能；自動分場的戰後結果預設 15 秒後隱藏，避免擋住原生介面。
- 外部 HUD 新增命令檔協定，所有互動仍與 Unreal UI 隔離，避免重回已知的 UE4SS UMG／`PrintString` 崩潰路徑。

## 0.5.0-visual-meter — 簡約比例長條 HUD

- 將戰鬥 HUD 從單一大段文字改為結構化 V2 狀態：每個技能都有獨立名稱、傷害、占比、施放次數、整場 DPS 與計時欄位。
- 新增依本場最高技能傷害正規化的橫向比例長條；右側固定顯示技能 DPS，次要數字縮成傷害、占比與施放次數。
- 公開預設改為 `compact`，面板約 360 像素寬；F1 的 `full` 模式仍會展開每次傷害、動作、施放 DPS、面板 CD、實際間隔與 AI 空窗。
- 未解析候選使用黃色長條提示；多個帕魯／人物來源會保留來源分組，不改變既有計算口徑。
- 舊版 V1 文字設定頁仍可顯示；v0.4.x 使用者設定只遷移一次到簡約模式，之後的選擇繼續自動保存。
- 新增 WPF V1／V2 執行期驗證，顯示層仍完全不呼叫 Unreal UMG 或 `PrintString`。

## 0.4.2-external-hud — 隔離所有 Unreal 畫面呼叫

- 實機確認 `PrintString` 也會在首次傷害後造成 UE4SS GameThread 存取違規，因此從即時傷害路徑完全移除動態 UMG 與 `PrintString`。
- 新增獨立透明置頂 HUD：Lua 只寫入 UTF-8 狀態檔，由隨附的 Windows WPF 顯示程序呈現，不呼叫任何 Unreal UI API。
- 保留 F1、方向鍵、多語言、人物傷害、資料密度、位置、縮放與戰後保留設定；Palworld 不在前景時面板自動隱藏，遊戲結束後程序自動退出。
- 新增外部 HUD PowerShell 語法、啟動／結束、Workshop 同步與禁用兩條崩潰路徑的回歸驗證。

## 0.4.1-safe-overlay — 修正首次傷害後崩潰

- 根據實機 CrashContext 與 UE4SS.log，停用會在 GameThread 造成存取違規的動態 UMG Widget 建立流程。
- F1 設定與多語言技能 DPS 改走具固定識別鍵的安全螢幕文字層，仍不使用聊天框。
- 新增回歸測試，禁止預設路徑呼叫實驗性 UMG 建立函式。

## 0.4.0-multilingual-hud — 介面與技能名稱同步切換

- F1 新增顯示語言，可選跟隨遊戲或 Palworld 的 17 種支援語言，切換後立即保存。
- 設定面板、技能 DPS HUD 與聊天核心文字會一起切換，不再只有繁中／英文二分。
- 內建 384 個技能代碼的 17 語言名稱表；指定 MOD 語言不同於遊戲語言時，技能名稱仍能正確切換。
- 新增可重複執行的 PalDB 名稱表更新工具，以及繁中、英文、日文即時切換回歸測試。

## 0.3.1-hud — 精簡技能名稱

- HUD 預設只顯示官方本地化技能名稱，避免每列同時塞入中文名稱與英文代碼。
- F1 新增「顯示內部英文代碼」，需要核對 Waza／動作代號時才開啟；缺少官方名稱時仍自動以代碼保底。

## 0.3.0-hud — 獨立技能 DPS 面板

- 新增 Unreal UMG 專用 HUD，戰鬥中即時顯示帕魯與逐技能傷害、整場 DPS、命中及施放計時；聊天輸出預設關閉。
- 新增 F1 設定面板，可調整人物傷害、資料密度、位置、縮放、戰後保留及聊天模式，並持久保存設定。
- 面板完整列出每次傷害、動作時間、單次施放 DPS、面板 CD、實際施放間隔與 AI／再用空窗。
- F1 被其他 UE4SS 模組占用時自動改用 Ctrl+F1；UMG 不相容時退回具固定識別鍵的螢幕文字，不使用聊天框。
- PalSchema 不是必要依賴；本 Mod 只依賴 UE4SS Experimental。

## 0.2.0-diagnostic — 技能完整實測

- 從遊戲本地化資料取得官方技能名稱，聊天同時顯示中文名稱與內部英文代碼。
- 從遊戲技能資料庫讀取面板 CD；目前實測技能另有中英名稱與 CD 保底對照。
- 追蹤 Waza 動作開始／結束，按實際施放實例統計施放次數、每次傷害、完整動作時間與單次施放 DPS。
- 計算相鄰施放的實際開始間隔、相對面板 CD 差值，以及上一動作結束到下一次開始的 AI／再用空窗。
- 聊天列出聚合結果，`UE4SS.log` 另寫每次施放明細與完整計時覆蓋率；缺失的生命週期資料不以首末命中窗冒充。
- 新增官方名稱、面板 CD、兩次完整動作、實際間隔與空窗的離線回歸測試。

## 0.1.2-diagnostic — 穩定技能分桶

- 修正每次技能施放的 UObject 實例編號被誤當成技能 ID，造成同一招拆成多列。
- 優先使用穩定化動作類別，將 `BP_ActionBeamSlicer_C_...` 等名稱合併為 `BeamSlicer`。
- 泛用 `ActionDamage` 只有在基礎威力與屬性於整場可唯一對應到一招時才併回該技能；歸屬有衝突則保留未解析。
- 傷害 schema 反射改為預設關閉，避免目前 UE4SS 版本的巢狀反射錯誤污染 log。
- 新增重複施放、唯一持續傷害回歸與模糊傷害拒絕歸屬測試。

## 0.1.1-diagnostic — Waza 技能歸屬修正

- 改用 `PalUtility:MakeDamageInfoByWazaType` 的 `EPalWazaID` 關聯每次帕魯技能傷害。
- 補抓 `BasePower` 與 `AttackElementType`，沒有 Waza 訊號時仍可分開未解析攻擊候選。
- 調整傷害 schema 巢狀反射流程；此功能仍屬選用診斷工具。
- 結算時直接在遊戲聊天欄列出逐技能傷害、占比、整場 DPS、命中與平均每擊。
- 新增 Waza 多段命中、技能切換與過期回退測試。

## 0.1.0-diagnostic — 獨立 Mod

- 建立獨立 `PalSkillDPSAnalyzerSP` 套件與全新 Workshop metadata，不沿用上游 Published File ID。
- 定位改為傷害驗證工具，不輸出玩家比較排名。
- 預設只納入帕魯；人物／武器來源可由 `IncludePlayerDamage` 選擇性開啟。
- 依技能欄位、投射物或傷害來源建立候選桶，輸出總傷害、占比、整場 DPS、命中與平均每擊。
- 啟動時反射傷害事件 schema，逐候選保留有界樣本；未知資料不猜名稱。
- 新增帕魯技能與人物武器候選回歸測試。

## 3.4.0

- 新增语义明确的 `EnablePalDamageBreakdown` 开关，用于显示玩家角色和每只参战帕鲁的伤害、占比及 DPS。
- 帕鲁明细继续优先显示玩家自定义昵称，未设置时回退物种本地化名称。
- 保留 `EnableTeamDetails` 作为旧配置兼容别名，两者任一开启即可显示同一份明细。
- 专用服务器默认关闭逐只帕鲁明细，避免公共聊天刷屏。
- 文档明确区分 Boss 战斗场地与 PvP 竞技场的支持边界。

## 1.2.0 — Steam 创意工坊单机版

- 单机版默认开启逐只帕鲁伤害明细，攻击 Boss 后无需再猜测旧配置项名称。
- 配置文件提供 `EnablePalDamageBreakdown` 独立开关，设为 `false` 可恢复精简结算。
- 创意工坊多语言说明补充开启方式和竞技场支持范围。

## 3.3.0

- 新增 Palworld 官方全部 17 种语言的游戏内核心战报。
- 单机/房主版通过 Unreal 当前语言自动选择翻译，无法识别时回退英语。
- 新增命名占位符模板，允许各语言独立调整语序，不再依赖中文字符串拼接。
- Boss 和帕鲁名称继续优先读取游戏本地化数据库；配置覆盖支持按语言分别设置。
- Steam 创意工坊单机版升级到 v1.1.0，新增 17 种本地化标题、描述和自动校验清单。
- 英语改为 Steam 页面正式回退语言，避免中文内容被错误写入英文栏。

## 3.2.0

- 新增可选 `BossDPSNativeCollector` C++ 采集器，高频回调只读取并聚合伤害字段。
- Lua 改为定时拉取聚合桶，继续负责 Boss 判断、玩家/帕鲁归属、捕捉/死亡结算和聊天战报。
- 普通目标经 Lua 首次判定后回传 C++ 负缓存，后续伤害不再进入 Lua。
- 原生反射或 ABI 不兼容时默认自动回退到原有 Lua 伤害钩子，并在启动日志中明确显示采集模式。
- 聚合记录保留精确总伤害和命中次数；新增 200 万次并发原生压力测试及 Lua 原生桥接回归。

## 1.1.0 — Steam 创意工坊单机版

- 新增符合 Palworld 1.0 `Info.json`/Lua 安装规则的创意工坊内容包，并显式依赖 `UE4SSExperimentalPW`。
- 新增 `LocalOnlyMessages`：单机/房主安装只向本机参与者显示战报，多人情况下无法确定本机身份时停止发送。
- 新增创意工坊封面、中文介绍、构建脚本和包结构验证。
- 新增 17 种游戏内语言以及对应的创意工坊标题和描述。

## 3.1.0

- 将月亮领主的身体、头部和左右部件合并为一场遭遇，只在主体结束时统一结算。
- 使用共同 Owner/父 Actor 隔离同时存在的复合 Boss；缺失关系时使用短加入窗口兜底。
- 调整伤害热路径，先判断受击目标是否为 Boss，再解析玩家与帕鲁归属。
- 增加普通目标负缓存，避免全服普通战斗反复执行完整归属查询。
- 缓存攻击来源、玩家身份和每场逐只帕鲁元数据，降低多段技能的游戏线程反射开销。
- 增加复合 Boss、并行复合遭遇、千次多段命中以及普通目标快速路径回归测试。

## 3.0.0

- 默认改为低打扰结算：摘要、MVP 和玩家综合排名。
- 默认保留一行开始统计提示，方便确认模组已识别 Boss 战。
- 新增 DPS 总开关以及开始提示、实时战况、详细奖项、队内明细和趣味点评的独立开关。
- 修复多人参与时同一消息按人数重复显示的问题。
- 修复旁观者收到其他队伍 Boss 战报的问题。
- 使用统一的 `PalUtility:PalCaptureSuccess` 捕捉入口并保留特殊战斗兼容监听。
- 无伤害超时缩短为 60 至 70 秒。
- 改进坐骑技能、玩家角色和逐只帕鲁伤害归属。
- 增加默认精简模式、开关隔离、收件数组和压力测试。
